{% layout "default" %}

<style>
	/* General Reset and Font */
	body {
		font-family: 'Inter', 'Segoe UI', Arial, sans-serif;
		background: #f8fafc;
		color: #222;
		margin: 0;
	}

	/* Header Styles */
	.header {
		background: linear-gradient(90deg, #2563eb 0%, #1e40af 100%);
		color: #fff;
		padding: 48px 0 32px 0;
		border-radius: 0 0 24px 24px;
		box-shadow: 0 4px 24px rgba(30, 64, 175, 0.08);
	}

	.header h1 {
		font-size: 2.5rem;
		font-weight: 700;
		letter-spacing: -1px;
		margin-bottom: 0.5rem;
	}

	.header p {
		font-size: 1.25rem;
		font-weight: 400;
		opacity: 0.9;
	}

	/* Features Section */
	.features {
		padding: 56px 0 32px 0;
	}

	.feature {
		background: #fff;
		border-radius: 16px;
		box-shadow: 0 2px 12px rgba(0, 0, 0, 0.04);
		padding: 32px 20px;
		text-align: center;
		transition: box-shadow 0.2s;
		margin-bottom: 32px;
	}

	.feature:hover {
		box-shadow: 0 6px 24px rgba(37, 99, 235, 0.10);
	}

	.feature i {
		font-size: 2.5rem;
		color: #2563eb;
		margin-bottom: 16px;
	}

	.feature h2 {
		font-size: 1.25rem;
		font-weight: 600;
		margin: 12px 0 8px 0;
	}

	.feature p {
		font-size: 1rem;
		color: #555;
		opacity: 0.85;
	}

	/* Call to Action */
	.call-to-action {
		text-align: center;
		padding: 48px 0 32px 0;
	}

	.btn-primary {
		border-radius: 999px;
		padding: 14px 40px;
		font-size: 1.1rem;
		font-weight: 600;
		background: linear-gradient(90deg, #2563eb 0%, #1e40af 100%);
		border: none;
		color: #fff;
		box-shadow: 0 2px 8px rgba(37, 99, 235, 0.10);
		transition: background 0.2s, box-shadow 0.2s;
		text-decoration: none;
		display: inline-block;
	}

	.btn-primary:hover {
		background: linear-gradient(90deg, #1e40af 0%, #2563eb 100%);
		box-shadow: 0 4px 16px rgba(37, 99, 235, 0.18);
		color: #fff;
	}

	/* Responsive Grid */
	@media (min-width: 768px) {
		.row {
			display: flex;
			gap: 24px;
		}

		.col-md-4 {
			flex: 1;
		}
	}
</style>

{%- comment -%}
Header: Brand and tagline
{%- endcomment -%}
<header class="header text-center">
	<div class="container">
		<h1>cURL</h1>
		<p class="lead">Shorten your links, amplify your reach.</p>
	</div>
</header>

{%- comment -%}
Features: Three key features with icons
{%- endcomment -%}
<section class="features">
	<div class="container">
		<div class="row">
			<div class="col-md-4">
				<div class="feature">
					<i class="bi bi-link"></i>
					<h2>Shorten URLs</h2>
					<p>Transform long URLs into concise, shareable links instantly.</p>
				</div>
			</div>
			<div class="col-md-4">
				<div class="feature">
					<i class="bi bi-graph-up"></i>
					<h2>Track Analytics</h2>
					<p>Monitor clicks and engagement to measure your link performance.</p>
				</div>
			</div>
			<div class="col-md-4">
				<div class="feature">
					<i class="bi bi-code-slash"></i>
					<h2>API Access</h2>
					<p>Integrate with your apps using secure, reliable API endpoints.</p>
				</div>
			</div>
		</div>
	</div>
</section>

{%- comment -%}
Call to Action: Main button to start
{%- endcomment -%}
<section class="call-to-action">
	<div class="container">
		<h2>Ready to get started?</h2>
		<a href="/home" class="btn btn-primary btn-lg">Shorten your link</a>
	</div>
</section>